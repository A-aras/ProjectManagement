import { ArrayLenghtPipe } from './arraylenght.pipe';

describe('ArrayLenghtPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayLenghtPipe();
    expect(pipe).toBeTruthy();
  });
});
